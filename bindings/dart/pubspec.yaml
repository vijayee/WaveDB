name: wavedb
version: 0.1.0
description: Dart FFI bindings for WaveDB - hierarchical key-value database

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.0
  meta: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0

platforms:
  linux:
  macos:
  windows:
